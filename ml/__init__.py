"""
RecoverIQ ML package.

Day 2: reproducible, leakage-free feature engineering and a time-aware
train/validation split for the recovery-prediction target.

Day 3: LogisticRegression model training (ml.training), evaluation,
model persistence (models/), and a reusable inference component
(ml.inference) that loads the persisted model and returns recovery
probabilities.

The /predict API endpoint and recovery decision engine are future work.
"""
